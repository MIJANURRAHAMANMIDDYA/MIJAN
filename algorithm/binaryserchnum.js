const accecs=require('../utility/allfunc')
  let arr1=[1,2,3,4,5];
  const input=require('readline-sync');
  let data=input.questionInt("enter the finding data:")
 accecs.Binarysearchnum(arr1,0,(arr1.length)-1,arr1.length,data);

    
